<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="muhammadhusen.com.ert">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".register.DaftarActivity"
            android:parentActivityName=".LoginActivity" />
        <activity android:name=".MenuActivity" />
        <activity
            android:name=".berita.homeActivity"
            android:parentActivityName=".MenuActivity" />
        <activity
            android:name=".berita.tulisBerita"
            android:parentActivityName=".MenuActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".berita.isiberita" />
        <activity
            android:name=".aspirasi.Aspirasi"
            android:label="@string/title_activity_aspirasi"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>